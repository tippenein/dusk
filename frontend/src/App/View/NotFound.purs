module App.View.NotFound where

import Prelude ((<>), ($))
-----
import App.Router (Location)
-----
import Halogen as H
import Halogen.HTML as HH

-- render :: Location -> H.ComponentHTML Query
-- render r = HH.text $ "Not found"
